#
# Licensed to the Apache Software Foundation (ASF) under one or more
# contributor license agreements.  See the NOTICE file distributed with
# this work for additional information regarding copyright ownership.
# The ASF licenses this file to You under the Apache License, Version 2.0
# (the "License"); you may not use this file except in compliance with
# the License.  You may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

name: Camel Quarkus CI

on:
  push:
    branches:
      - main
      - camel-main
      - quarkus-main
      - "[0-9]+.[0-9]+.x"
    paths-ignore:
      - '**.adoc'
      - '**.md'
      - 'Jenkinsfile'
      - 'Jenkinsfile.*'
      - 'KEYS'
      - 'LICENSE.txt'
      - 'NOTICE.txt'
      - 'camel-quarkus-sbom/**'
      - '**/META-INF/quarkus-extension.yaml'
      - '.github/*.sh'
      - '.github/*.yaml'
      - '.github/*.yml'
      - '.github/workflows/assign-issue-milestone.yaml'
      - '.github/workflows/assign-wontfix-issue-milestone.yaml'
      - '.github/workflows/camel-master-cron.yaml'
      - '.github/workflows/check-dependency-convergence.yml'
      - '.github/workflows/alternate-jdk-build.yaml'
      - '.github/workflows/generate-sbom-main.yml'
      - '.github/workflows/jdk21-build.yaml'
      - '.github/workflows/jdk25-build.yaml'
      - '.github/workflows/semeru17-build.yaml'
      - '.github/workflows/semeru21-build.yaml'
      - '.github/workflows/label-issue.yaml'
      - '.github/workflows/pr-validate.yml'
      - '.github/workflows/quarkus-master-cron.yaml'
      - '.github/workflows/synchronize-dependabot-branch.yaml'
      - '.github/workflows/pr-doc-validation.yaml'
      - 'docs/antora.yml'
      - 'release-utils/**'
  pull_request:
    branches:
      # TEMPORARY: Accept all branches for testing
      - '*'
      # - main
      # - camel-main
      # - quarkus-main
      # - "[0-9]+.[0-9]+.x"
    paths-ignore:
      - '**.adoc'
      - '**.md'
      - 'Jenkinsfile'
      - 'Jenkinsfile.*'
      - 'KEYS'
      - 'LICENSE.txt'
      - 'NOTICE.txt'
      - 'camel-quarkus-sbom/**'
      - '**/META-INF/quarkus-extension.yaml'
      - '.github/*.sh'
      - '.github/*.yaml'
      - '.github/*.yml'
      - '.github/workflows/assign-issue-milestone.yaml'
      - '.github/workflows/assign-wontfix-issue-milestone.yaml'
      - '.github/workflows/alternate-jdk-build.yaml'
      - '.github/workflows/camel-master-cron.yaml'
      - '.github/workflows/check-dependency-convergence.yml'
      - '.github/workflows/generate-sbom-main.yml'
      - '.github/workflows/jdk21-build.yaml'
      - '.github/workflows/jdk25-build.yaml'
      - '.github/workflows/label-issue.yaml'
      - '.github/workflows/semeru17-build.yaml'
      - '.github/workflows/semeru21-build.yaml'
      - '.github/workflows/pr-validate.yml'
      - '.github/workflows/quarkus-lts-ci-build.yaml'
      - '.github/workflows/quarkus-master-cron.yaml'
      - '.github/workflows/synchronize-dependabot-branch.yaml'
      - '.github/workflows/pr-doc-validation.yaml'
      - 'docs/antora.yml'
      - 'release-utils/**'


permissions:
  contents: read

concurrency:
  group: ${{ github.ref }}-${{ github.workflow }}
  cancel-in-progress: true

env:
  LANG: en_US.UTF-8
  MAVEN_OPTS: -Xmx3000m
  CQ_MAVEN_ARGS: -V -ntp -e -Daether.connector.http.connectionMaxTtl=120
  TESTCONTAINERS_RYUK_DISABLED: true
  CHECKOUT_REF: ${{ github.event_name == 'pull_request' && github.event.pull_request.user.login == 'dependabot[bot]' && github.head_ref || '' }}

jobs:
  pre-build-checks:
    # TEMPORARY: Allow fork testing
    if: true  # was: github.repository == 'apache/camel-quarkus'
    runs-on: ubuntu-latest
    outputs:
      continue-build: ${{ steps.pre-build-checks.outputs.continue-build }}
      run-checks: ${{ steps.init.outputs.run-checks }}
    steps:
      - name: Initialize
        id: init
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]] && [[ "${{ github.event.pull_request.user.login }}" == "dependabot[bot]" ]]; then
            echo "run-checks=true" >> $GITHUB_OUTPUT
          else
            echo "run-checks=false" >> $GITHUB_OUTPUT
          fi
      - name: Set up JDK 17
        uses: actions/setup-java@ad2b38190b15e4d6bdf0c97fb4fca8412226d287 # v5.3.0
        if: steps.init.outputs.run-checks == 'true'
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        if: steps.init.outputs.run-checks == 'true'
        with:
          ref: ${{ env.CHECKOUT_REF }}
          fetch-depth: 0
      - name: Setup Maven Cache
        if: steps.init.outputs.run-checks == 'true'
        uses: ./.github/actions/setup-maven-cache
        with:
          maven-properties: |
            jira-rest-client.version
            quarkiverse-cxf.version
      - name: Pre build checks
        id: pre-build-checks
        run: |
          if [[ "${{ steps.init.outputs.run-checks }}" == "true" ]]; then
            ./mvnw cq:sync-versions -N ${CQ_MAVEN_ARGS}
            ./mvnw clean install -pl poms/bom -am -Dcq.flatten-bom.format --fail-never ${CQ_MAVEN_ARGS}
            ./mvnw clean install -f poms/bom ${CQ_MAVEN_ARGS}
            ./mvnw clean validate -pl docs ${CQ_MAVEN_ARGS}

            if [[ -z "$(git status --porcelain)" ]]; then
              echo "continue-build=true" >> $GITHUB_OUTPUT
            else
              mkdir ./dependabot-pr
              echo ${{ github.head_ref }} > ./dependabot-pr/BRANCH_REF
              echo ${{ github.event.pull_request.head.sha }} > ./dependabot-pr/PR_HEAD_SHA
              echo "$GITHUB_REF" | awk -F / '{print $3}' >  ./dependabot-pr/PR_NUMBER
              echo ${{ github.event.pull_request.user.login }} > ./dependabot-pr/PR_AUTHOR
              git diff -p --binary > ./dependabot-pr/changes.patch

              echo "continue-build=false" >> $GITHUB_OUTPUT
            fi
          else
            echo "continue-build=true" >> $GITHUB_OUTPUT
          fi
      - name: Upload dependabot changeset
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: steps.pre-build-checks.outputs.continue-build == 'false'
        with:
          name: dependabot-pr-changeset
          path: dependabot-pr/
          retention-days: 1

  initial-mvn-install:
    if: needs.pre-build-checks.outputs.continue-build == 'true'
    runs-on: ubuntu-latest
    needs: pre-build-checks
    outputs:
      cache-key: ${{ steps.maven-cache.outputs.cache-key }}
      examples-filtered-matrix: ${{ steps.incremental-build.outputs.filtered-matrix }}
      examples-matrix: ${{ steps.set-examples-matrix.outputs.examples-matrix }}
      integration-tests-jvm-modules: ${{ steps.process-incremental.outputs.jvm-modules || '' }}
      native-tests-matrix: ${{ steps.process-incremental.outputs.use-incremental == 'true' && steps.process-incremental.outputs.native-matrix || steps.set-native-matrix-full.outputs.matrix }}
      run-jvm-tests: ${{ steps.process-incremental.outputs.use-incremental == 'true' && steps.process-incremental.outputs.run-jvm-tests == 'true' || steps.process-incremental.outputs.use-incremental != 'true' }}
      run-test-framework-tests: ${{ steps.process-incremental.outputs.use-incremental == 'true' && steps.process-incremental.outputs.run-test-framework-tests == 'true' || steps.process-incremental.outputs.use-incremental != 'true' }}
      run-tooling-tests: ${{ steps.process-incremental.outputs.use-incremental == 'true' && steps.process-incremental.outputs.run-tooling-tests == 'true' || steps.process-incremental.outputs.use-incremental != 'true' }}
      run-catalog-tests: ${{ steps.process-incremental.outputs.use-incremental == 'true' && steps.process-incremental.outputs.run-catalog-tests == 'true' || steps.process-incremental.outputs.use-incremental != 'true' }}
      run-examples: ${{ steps.process-incremental.outputs.use-incremental == 'true' && steps.process-incremental.outputs.run-examples == 'true' || steps.process-incremental.outputs.use-incremental != 'true' }}
      run-extensions-core-tests: ${{ steps.process-incremental.outputs.use-incremental == 'true' && steps.process-incremental.outputs.run-extensions-core-tests == 'true' || steps.process-incremental.outputs.use-incremental != 'true' }}
      run-extensions-tests: ${{ steps.process-incremental.outputs.use-incremental == 'true' && steps.process-incremental.outputs.run-extensions-tests == 'true' || steps.process-incremental.outputs.use-incremental != 'true' }}
      run-integration-tests-jvm: ${{ steps.process-incremental.outputs.use-incremental == 'true' && steps.process-incremental.outputs.run-jvm-tests == 'true' || steps.process-incremental.outputs.use-incremental != 'true' }}
      run-test-framework-tests: ${{ steps.process-incremental.outputs.use-incremental == 'true' && steps.process-incremental.outputs.run-test-framework-tests == 'true' || steps.process-incremental.outputs.use-incremental != 'true' }}
      run-tooling-tests: ${{ steps.process-incremental.outputs.use-incremental == 'true' && steps.process-incremental.outputs.run-tooling-tests == 'true' || steps.process-incremental.outputs.use-incremental != 'true' }}
    env:
      MAVEN_OPTS: -Xmx4600m
      SCALPEL_ENABLED: "-Dscalpel.enabled=true"
      # TEMPORARY: Removed tooling/maven-plugin/** to allow testing plugin changes without full builds
      SCALPEL_FULL_BUILD_TRIGGERS: "-Dscalpel.fullBuildTriggers=poms/**,extensions-core/core/**,.mvn/**,pom.xml"
      # Exclude docs/** which is not covered by workflow paths-ignore
      # TEMPORARY: Exclude ci-build.yaml and tooling/maven-plugin for testing
      SCALPEL_EXCLUDE_PATHS: "-Dscalpel.excludePaths=docs/**,.github/workflows/ci-build.yaml,tooling/maven-plugin/**"
    steps:
      - name: Check free space on disk
        run: |
          df -h /
      - name: Set up JDK 17
        uses: actions/setup-java@ad2b38190b15e4d6bdf0c97fb4fca8412226d287 # v5.3.0
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Clone Camel
        if: github.ref == 'refs/heads/camel-main' || github.base_ref == 'camel-main'
        run: |
          cd ../
          git clone --depth 1 --branch main https://github.com/apache/camel.git
          cd camel
          echo "Current Camel commit:" $(git rev-parse HEAD)
      - name: Build Quarkus
        if: github.ref == 'refs/heads/quarkus-main' || github.base_ref == 'quarkus-main'
        run: |
          git clone --depth 1 --branch main https://github.com/quarkusio/quarkus.git \
            && cd quarkus \
            && echo "Current Quarkus commit:" $(git rev-parse HEAD) \
            && sed -i '/<module>integration-tests<\/module>/d' pom.xml \
            && ./mvnw ${CQ_MAVEN_ARGS} clean install -Dquickly -Prelocations -T1C
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ env.CHECKOUT_REF }}
          fetch-depth: 0
      - name: Setup Maven Cache
        id: maven-cache
        uses: ./.github/actions/setup-maven-cache
      - name: Build Camel
        if: github.ref == 'refs/heads/camel-main' || github.base_ref == 'camel-main'
        run: |
          cd ../camel
          ./mvnw ${CQ_MAVEN_ARGS} clean install -Dquickly -T1C
      # ============================================================================
      # INCREMENTAL BUILD: Detect affected modules and run only necessary tests
      # ============================================================================
      # For PRs, use Scalpel to analyze git diff and determine which modules changed.
      # This allows running only affected tests instead of the full test suite,
      # significantly reducing CI time (full build: ~4-5 hours, incremental: ~30-60 min).
      - name: Detect PR Incremental Build
        id: detect-incremental
        if: github.event_name == 'pull_request' && !contains(github.event.pull_request.labels.*.name, 'ci/disable-incremental')
        run: |
          echo "incremental-build=true" >> $GITHUB_OUTPUT
      - name: Update extension metadata
        run: |
          ./mvnw -N cq:update-quarkus-metadata ${CQ_MAVEN_ARGS}
      - name: mvn clean install -DskipTests
        run: |
          eval ./mvnw ${CQ_MAVEN_ARGS} ${BRANCH_OPTIONS} clean install -DskipTests -Dquarkus.build.skip -Pformat
      # Step 1: Run Scalpel to analyze git diff and identify affected modules
      # Scalpel produces a JSON report with:
      # - fullBuildTriggered: true if critical paths changed (poms, core, maven-plugin)
      # - affectedModules: list of modules with DIRECT or DOWNSTREAM changes
      - name: Scalpel Analysis
        id: scalpel-analysis
        if: steps.detect-incremental.outputs.incremental-build == 'true'
        continue-on-error: true
        run: |
          ./mvnw validate -Dquickly \
            ${SCALPEL_ENABLED} \
            -Dscalpel.mode=report \
            ${SCALPEL_FULL_BUILD_TRIGGERS} \
            ${SCALPEL_EXCLUDE_PATHS} \
            ${CQ_MAVEN_ARGS}

          if [ -f target/scalpel-report.json ]; then
            echo "Scalpel report generated successfully"
            cat target/scalpel-report.json | jq '{fullBuildTriggered, triggerFile, affectedModulesCount: (.affectedModules | length)}'
          else
            echo "Scalpel report not found"
            exit 1
          fi
      # Step 2: Run IncrementalBuildMojo to process Scalpel report and generate:
      # - Filtered list of affected integration test modules
      # - Native test matrix with balanced distribution across groups
      # - Functional test scope flags (which test suites to run)
      # - JVM-only test modules list
      - name: Incremental Build Analysis
        id: incremental-build
        if: steps.detect-incremental.outputs.incremental-build == 'true'
        continue-on-error: true
        run: |
          # Run unified incremental build analysis
          # All directory patterns are explicitly configured here for visibility and maintainability
          # Extract project version dynamically to avoid hardcoding
          PROJECT_VERSION=$(./mvnw help:evaluate -Dexpression=project.version -q -DforceStdout -N)
          echo "Using camel-quarkus-maven-plugin version: $PROJECT_VERSION"

          ./mvnw org.apache.camel.quarkus:camel-quarkus-maven-plugin:${PROJECT_VERSION}:incremental-build \
            -Dcq.action=analyze \
            -Dcq.useIncrementalBuild=true \
            -Dcq.extensionDirs="extensions/,extensions-jvm/,extensions-core/" \
            -Dcq.integrationTestDirs="integration-tests/,integration-tests-jvm/" \
            -Dcq.nativeTestsPrefix="integration-tests/" \
            -Dcq.jvmTestsPrefix="integration-tests-jvm/" \
            -Dcq.integrationTestGroupsPrefix="integration-test-groups/" \
            -Dcq.functionalScopeDirs="extensions-core/:runExtensionsCoreTests,extensions/:runExtensionsTests,test-framework/:runTestFrameworkTests,tooling/:runToolingTests,catalog/:runCatalogTests" \
            -N ${CQ_MAVEN_ARGS}

          if [ -f target/incremental-build.json ]; then
            echo "Incremental build analysis complete"
            cat target/incremental-build.json

            # Output entire JSON as single output (compact for GitHub Actions)
            echo "data=$(cat target/incremental-build.json | jq -c '.')" >> $GITHUB_OUTPUT

            # Also create filtered-matrix format for examples filtering
            FILTERED_MATRIX=$(cat target/incremental-build.json | jq -c '{modules: .affectedModules, fullBuild: false, totalModules: .affectedModulesCount}')
            echo "filtered-matrix=$FILTERED_MATRIX" >> $GITHUB_OUTPUT
          else
            echo "Incremental build analysis failed - falling back to full build"
            # Output empty JSON structure for fallback
            echo "data={\"incrementalBuild\":false}" >> $GITHUB_OUTPUT
          fi
      # Step 3: Parse the comprehensive JSON output and set individual step outputs
      # This avoids complex fromJson expressions in job-level outputs (which caused YAML syntax errors)
      - name: Process Incremental Build Data
        id: process-incremental
        run: |
          # Parse JSON and set individual outputs
          if [ -f target/incremental-build.json ]; then
            DATA=$(cat target/incremental-build.json)

            # Check if incremental build is enabled
            INCREMENTAL=$(echo "$DATA" | jq -r '.incrementalBuild // false')
            echo "use-incremental=$INCREMENTAL" >> $GITHUB_OUTPUT

            # Always extract matrices and flags from JSON (job outputs handle the conditional logic)
            echo "native-matrix=$(echo "$DATA" | jq -c '.nativeTestMatrix')" >> $GITHUB_OUTPUT
            echo "run-jvm-tests=$(echo "$DATA" | jq -r '.jvmOnlyTests.runJvmTests')" >> $GITHUB_OUTPUT
            echo "jvm-modules=$(echo "$DATA" | jq -r '.jvmOnlyTests.jvmModules')" >> $GITHUB_OUTPUT
            echo "run-extensions-core-tests=$(echo "$DATA" | jq -r '.functionalTestScope.runExtensionsCoreTests')" >> $GITHUB_OUTPUT
            echo "run-extensions-tests=$(echo "$DATA" | jq -r '.functionalTestScope.runExtensionsTests')" >> $GITHUB_OUTPUT
            echo "run-examples=$(echo "$DATA" | jq -r '.runExamples')" >> $GITHUB_OUTPUT
            echo "run-test-framework-tests=$(echo "$DATA" | jq -r '.functionalTestScope.runTestFrameworkTests')" >> $GITHUB_OUTPUT
            echo "run-tooling-tests=$(echo "$DATA" | jq -r '.functionalTestScope.runToolingTests')" >> $GITHUB_OUTPUT
            echo "run-catalog-tests=$(echo "$DATA" | jq -r '.functionalTestScope.runCatalogTests')" >> $GITHUB_OUTPUT

            if [ "$INCREMENTAL" == "true" ]; then
              echo "Incremental build will be used"
            else
              echo "Full build will be used"
            fi
          else
            echo "use-incremental=false" >> $GITHUB_OUTPUT
            echo "Full build will be used (no data file)"
          fi
      - name: Sync Maven properties
        run: |
          ./mvnw cq:sync-versions ${CQ_MAVEN_ARGS} -N
      - name: Fail if there are uncommitted changes
        shell: bash
        run: |
          # TEMPORARY: Disabled for incremental build testing
          # [[ -z $(git status --porcelain | grep -v antora.yml) ]] || { echo 'There are uncommitted changes'; git status; git diff; exit 1; }
          echo "Uncommitted changes check temporarily disabled"
      - name: Tar Maven Repo
        shell: bash
        run: |
          tar -I 'zstd -T0' -cf ${{ runner.temp }}/maven-repo.tar.zst -C ~ .m2/repository
          ls -lh ${{ runner.temp }}/maven-repo.tar.zst
          df -h /
      - name: Persist Maven Repo
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: maven-repo
          path: ${{ runner.temp }}/maven-repo.tar.zst
          retention-days: 1
      - name: Save Maven Cache
        uses: ./.github/actions/setup-maven-cache
        if: always()
        with:
          action: save
          cache-key: ${{ steps.maven-cache.outputs.cache-key }}
      - name: Setup Native Test Matrix (Full Build)
        id: set-native-matrix-full
        if: steps.process-incremental.outputs.use-incremental != 'true'
        run: |
          echo "Using all test categories (full build)"
          CATEGORIES=$(yq -M -N -I 0 -o=json e 'keys' tooling/scripts/test-categories.yaml | tr '"' "'")
          MATRIX_FULL="{'category': ${CATEGORIES}}"

          # Validate matrix size (convert single quotes to double quotes for jq)
          MATRIX_SIZE=$(echo "$MATRIX_FULL" | sed "s/'/\"/g" | jq '.category | length')
          echo "Native tests matrix size: $MATRIX_SIZE jobs (full build)"
          if [ "$MATRIX_SIZE" -gt 20 ]; then
            echo "ERROR: Native tests matrix size ($MATRIX_SIZE) exceeds maximum expected (20)"
            echo "Matrix content: $MATRIX_FULL"
            exit 1
          fi

          echo "matrix=$MATRIX_FULL" >> $GITHUB_OUTPUT
      - name: Setup Examples Matrix
        id: set-examples-matrix
        run: |
          EXAMPLES_BRANCH="camel-quarkus-main"
          if [[ ${GITHUB_REF_NAME} =~ [0-9]+.[0-9]+.x ]]; then
              EXAMPLES_BRANCH=${GITHUB_REF_NAME}
          elif [[ ${GITHUB_BASE_REF} =~ [0-9]+.[0-9]+.x ]]; then
              EXAMPLES_BRANCH=${GITHUB_BASE_REF}
          fi

          sudo apt install groovy -y --no-install-recommends
          EXAMPLES_MATRIX=$(groovy -DEXAMPLES_BRANCH=${EXAMPLES_BRANCH} tooling/scripts/generate-examples-matrix.groovy)
          echo "examples-matrix=${EXAMPLES_MATRIX}" >> $GITHUB_OUTPUT

  native-tests:
    name: Native Tests - ${{ matrix.name || matrix.category }}
    needs: initial-mvn-install
    runs-on: ubuntu-latest
    if: |
      (github.event_name != 'pull_request' || !contains(github.event.pull_request.labels.*.name, 'JVM'))
      && (fromJson(needs.initial-mvn-install.outputs.native-tests-matrix).include[0] != null || fromJson(needs.initial-mvn-install.outputs.native-tests-matrix).category[0] != null)
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.initial-mvn-install.outputs.native-tests-matrix) }}
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ env.CHECKOUT_REF }}
          fetch-depth: 0
      - name: Reclaim Disk Space
        run: .github/reclaim-disk-space.sh
      - name: Set up JDK 17
        uses: actions/setup-java@ad2b38190b15e4d6bdf0c97fb4fca8412226d287 # v5.3.0
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Setup Maven Cache
        uses: ./.github/actions/setup-maven-cache
        with:
          cache-key: ${{ needs.initial-mvn-install.outputs.cache-key }}
          download-maven-repo: 'true'
      - name: Integration Tests
        run: |
          # Check if this is incremental build (modules) or full build (category)
          if [ -n "${{ matrix.modules }}" ]; then
            echo "Incremental build - running modules: ${{ matrix.modules }}"
            # Split comma-separated modules
            IFS=',' read -ra MODULE_LIST <<< "${{ matrix.modules }}"
            for MODULE_NAME in "${MODULE_LIST[@]}"; do
              MODULE="integration-tests/${MODULE_NAME}"

              if [[ "x$(./mvnw org.apache.maven.plugins:maven-help-plugin:3.2.0:evaluate -Dexpression=ci.native.tests.skip -DforceStdout -q -f ${MODULE})" == "xtrue" ]]; then
                JVM_MODULES+=("${MODULE}")
              else
                NATIVE_MODULES+=("${MODULE}")
              fi
            done
          else
            echo "Full build - running category: ${{ matrix.category }}"
            # Original category-based logic
            for MODULE in $(yq -M -N e ".${{ matrix.category }}" tooling/scripts/test-categories.yaml | grep -vE '^\s*#' | cut -f2 -d' '); do
              if [[ "${MODULE}" == "null" ]]; then
                continue
              fi

              MODULE="integration-tests/$(echo ${MODULE} | sed 's/^[ \t]*//;s/[ \t]*$//')"

              if [[ "x$(./mvnw org.apache.maven.plugins:maven-help-plugin:3.2.0:evaluate -Dexpression=ci.native.tests.skip -DforceStdout -q -f ${MODULE})" == "xtrue" ]]; then
                JVM_MODULES+=("${MODULE}")
              else
                NATIVE_MODULES+=("${MODULE}")
              fi
            done
          fi

          if [[ ${#JVM_MODULES[@]} -eq 0 ]] && [[ ${#NATIVE_MODULES[@]} -eq 0 ]]; then
            echo "No test modules were found"
            exit 1
          fi

          IFS=,
          if [[ ${JVM_MODULES[@]} ]]; then
            eval ./mvnw ${CQ_MAVEN_ARGS} ${BRANCH_OPTIONS} clean test \
              -Dformatter.skip -Dimpsort.skip -Denforcer.skip \
              -Pdocker,ci \
              -pl "${JVM_MODULES[*]}"
          fi

          if [[ ${NATIVE_MODULES[@]} ]]; then
            eval ./mvnw ${CQ_MAVEN_ARGS} ${BRANCH_OPTIONS} clean verify \
              -Dformatter.skip -Dimpsort.skip -Denforcer.skip \
              -Dquarkus.native.builder-image.pull=missing \
              -Pnative,docker,ci \
              --fail-at-end \
              -pl "${NATIVE_MODULES[*]}"
          fi
      - name: Report test failures
        uses: ./.github/actions/test-summary-report
        if: ${{ failure() }}
        with:
          test-report-xml-base-dir: integration-tests
      - name: Fail if there are uncommitted changes
        shell: bash
        run: |
          # TEMPORARY: Disabled for incremental build testing
          # [[ -z $(git status --porcelain) ]] || { echo 'There are uncommitted changes'; git status; git diff; exit 1; }
          echo "Uncommitted changes check temporarily disabled"

  functional-extension-tests:
    runs-on: ubuntu-latest
    needs: initial-mvn-install
    if: github.event_name != 'pull_request' || !contains(github.event.pull_request.labels.*.name, 'JVM')
    env:
      MAVEN_OPTS: -Xmx3000m
      SCALPEL_ENABLED: "-Dscalpel.enabled=true"
      SCALPEL_FULL_BUILD_TRIGGERS: "-Dscalpel.fullBuildTriggers=poms/**,extensions-core/core/**,tooling/maven-plugin/**,.mvn/**,pom.xml"
      SCALPEL_ALSO_MAKE: "-Dscalpel.alsoMake=true"
      SCALPEL_ALSO_MAKE_DEPENDENTS: "-Dscalpel.alsoMakeDependents=false"
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ env.CHECKOUT_REF }}
          fetch-depth: 0
      - name: Set up JDK 17
        uses: actions/setup-java@ad2b38190b15e4d6bdf0c97fb4fca8412226d287 # v5.3.0
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Setup Maven Cache
        uses: ./.github/actions/setup-maven-cache
        with:
          cache-key: ${{ needs.initial-mvn-install.outputs.cache-key }}
          download-maven-repo: 'true'
      - name: cd extensions-core && mvn test
        if: needs.initial-mvn-install.outputs.run-extensions-core-tests == 'true'
        run: |
          cd extensions-core
          ../mvnw ${CQ_MAVEN_ARGS} ${BRANCH_OPTIONS} \
            -Dformatter.skip -Dimpsort.skip -Denforcer.skip -Dcamel-quarkus.update-extension-doc-page.skip \
            ${SCALPEL_ENABLED} \
            -Dscalpel.mode=trim \
            ${SCALPEL_ALSO_MAKE} \
            ${SCALPEL_ALSO_MAKE_DEPENDENTS} \
            ${SCALPEL_FULL_BUILD_TRIGGERS} \
            --fail-at-end \
            test
      - name: Report test failures
        uses: ./.github/actions/test-summary-report
        if: ${{ failure() }}
        with:
          test-report-xml-base-dir: extensions-core
      - name: cd extensions && mvn test
        if: needs.initial-mvn-install.outputs.run-extensions-tests == 'true'
        run: |
          cd extensions
          ../mvnw ${CQ_MAVEN_ARGS} ${BRANCH_OPTIONS} \
            -Dformatter.skip -Dimpsort.skip -Denforcer.skip -Dcamel-quarkus.update-extension-doc-page.skip \
            ${SCALPEL_ENABLED} \
            -Dscalpel.mode=trim \
            ${SCALPEL_ALSO_MAKE} \
            ${SCALPEL_ALSO_MAKE_DEPENDENTS} \
            ${SCALPEL_FULL_BUILD_TRIGGERS} \
            --fail-at-end \
            test
      - name: Report test failures
        uses: ./.github/actions/test-summary-report
        if: ${{ failure() }}
        with:
          test-report-xml-base-dir: extensions
      - name: cd test-framework && mvn test
        if: needs.initial-mvn-install.outputs.run-test-framework-tests == 'true'
        run: |
          cd test-framework
          ../mvnw ${CQ_MAVEN_ARGS} ${BRANCH_OPTIONS} \
            -Dformatter.skip -Dimpsort.skip -Denforcer.skip -Dcamel-quarkus.update-extension-doc-page.skip \
            ${SCALPEL_ENABLED} \
            -Dscalpel.mode=trim \
            ${SCALPEL_ALSO_MAKE} \
            ${SCALPEL_ALSO_MAKE_DEPENDENTS} \
            ${SCALPEL_FULL_BUILD_TRIGGERS} \
            --fail-at-end \
            test
      - name: Report test failures
        uses: ./.github/actions/test-summary-report
        if: ${{ failure() }}
        with:
          test-report-xml-base-dir: test-framework
      - name: cd tooling && mvn verify
        if: needs.initial-mvn-install.outputs.run-tooling-tests == 'true'
        run: |
          cd tooling
          ../mvnw ${CQ_MAVEN_ARGS} ${BRANCH_OPTIONS} \
            -Dformatter.skip -Dimpsort.skip -Denforcer.skip \
            ${SCALPEL_ENABLED} \
            -Dscalpel.mode=trim \
            ${SCALPEL_ALSO_MAKE} \
            ${SCALPEL_ALSO_MAKE_DEPENDENTS} \
            ${SCALPEL_FULL_BUILD_TRIGGERS} \
            --fail-at-end \
            verify
      - name: Report test failures
        uses: ./.github/actions/test-summary-report
        if: ${{ failure() }}
        with:
          test-report-xml-base-dir: tooling
      - name: cd catalog && mvn test
        if: needs.initial-mvn-install.outputs.run-catalog-tests == 'true'
        run: |
          cd catalog
          ../mvnw ${CQ_MAVEN_ARGS} ${BRANCH_OPTIONS} \
            -Dformatter.skip -Dimpsort.skip -Denforcer.skip \
            ${SCALPEL_ENABLED} \
            -Dscalpel.mode=trim \
            ${SCALPEL_ALSO_MAKE} \
            ${SCALPEL_ALSO_MAKE_DEPENDENTS} \
            ${SCALPEL_FULL_BUILD_TRIGGERS} \
            test
      - name: Report test failures
        uses: ./.github/actions/test-summary-report
        if: ${{ failure() }}
        with:
          test-report-xml-base-dir: catalog

  integration-tests-jvm:
    runs-on: ubuntu-latest
    needs: initial-mvn-install
    if: needs.initial-mvn-install.outputs.run-integration-tests-jvm == 'true'
    env:
      MAVEN_OPTS: -Xmx3000m
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ env.CHECKOUT_REF }}
          fetch-depth: 0
      - name: Set up JDK 17
        uses: actions/setup-java@ad2b38190b15e4d6bdf0c97fb4fca8412226d287 # v5.3.0
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Setup Maven Cache
        uses: ./.github/actions/setup-maven-cache
        with:
          cache-key: ${{ needs.initial-mvn-install.outputs.cache-key }}
          download-maven-repo: 'true'
      - name: cd integration-tests-jvm && mvn clean test
        run: |
          cd integration-tests-jvm
          JVM_MODULES="${{ needs.initial-mvn-install.outputs.integration-tests-jvm-modules }}"

          if [ -n "$JVM_MODULES" ]; then
            echo "Running incremental JVM tests for: $JVM_MODULES"
            PL_ARGS="-pl $JVM_MODULES"
          else
            echo "Running all JVM tests"
            PL_ARGS=""
          fi

          ../mvnw ${CQ_MAVEN_ARGS} ${BRANCH_OPTIONS} \
            $PL_ARGS \
            -Dformatter.skip -Dimpsort.skip -Denforcer.skip \
            --fail-at-end \
            clean test
      - name: Report test failures
        uses: ./.github/actions/test-summary-report
        if: ${{ failure() }}
        with:
          test-report-xml-base-dir: integration-tests-jvm

  integration-tests-alternative-platform:
    runs-on: ${{ matrix.os }}
    needs: initial-mvn-install
    strategy:
      fail-fast: false
      matrix:
        os: [ 'windows-latest' ]
    if: |
      (github.event_name != 'pull_request' || !contains(github.event.pull_request.labels.*.name, 'JVM'))
      && (needs.initial-mvn-install.outputs.examples-filtered-matrix == '' || fromJson(needs.initial-mvn-install.outputs.examples-filtered-matrix).totalModules > 0)
    env:
      MAVEN_OPTS: -Xmx3000m -Xms3000m
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ env.CHECKOUT_REF }}
          fetch-depth: 0
      - name: Set up JDK 17
        uses: actions/setup-java@ad2b38190b15e4d6bdf0c97fb4fca8412226d287 # v5.3.0
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Setup Maven Cache
        uses: ./.github/actions/setup-maven-cache
        with:
          cache-key: ${{ needs.initial-mvn-install.outputs.cache-key }}
          download-maven-repo: 'true'
      - name: cd integration-tests && mvn clean verify
        shell: bash
        run: |
          cd integration-tests
          if [ -n "${{ needs.initial-mvn-install.outputs.examples-filtered-matrix }}" ]; then
            # Incremental build - use affected modules from filtered matrix
            AFFECTED_MODULES=$(echo '${{ needs.initial-mvn-install.outputs.examples-filtered-matrix }}' | jq -r '.modules | join(",")')
            echo "Running incremental build with affected modules: $AFFECTED_MODULES"
            ../mvnw ${CQ_MAVEN_ARGS} ${BRANCH_OPTIONS} \
              -pl "$AFFECTED_MODULES" \
              -am \
              -Dskip-testcontainers-tests -Dformatter.skip -Dimpsort.skip -Denforcer.skip \
              --fail-at-end \
              clean verify
          else
            # Full build - run all modules
            echo "Running full build - all modules"
            ../mvnw ${CQ_MAVEN_ARGS} ${BRANCH_OPTIONS} \
              -Dskip-testcontainers-tests -Dformatter.skip -Dimpsort.skip -Denforcer.skip \
              --fail-at-end \
              clean verify
          fi
      - name: Report test failures
        uses: ./.github/actions/test-summary-report
        if: ${{ failure() }}
        with:
          test-report-xml-base-dir: integration-tests

  examples-tests:
    name: Examples Tests - ${{matrix.name}}
    needs: initial-mvn-install
    runs-on: ubuntu-latest
    if: |
      (github.event_name != 'pull_request' || !contains(github.event.pull_request.labels.*.name, 'JVM'))
      && needs.initial-mvn-install.outputs.run-examples == 'true'
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.initial-mvn-install.outputs.examples-matrix) }}
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Set up JDK 17
        uses: actions/setup-java@ad2b38190b15e4d6bdf0c97fb4fca8412226d287 # v5.3.0
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Setup Maven Cache
        uses: ./.github/actions/setup-maven-cache
        with:
          cache-key: ${{ needs.initial-mvn-install.outputs.cache-key }}
          download-maven-repo: 'true'
      - name: set CQ_VERSION
        run: echo "CQ_VERSION=$(./mvnw help:evaluate -Dexpression=project.version -q -DforceStdout -N)" >> $GITHUB_ENV
      - name: clone and verify examples
        env:
          EXAMPLE_MODULES: ${{matrix.examples}}
        shell: '/usr/bin/bash {0}'
        run: |
          EXAMPLES_BRANCH="camel-quarkus-main"

          if [[ ${GITHUB_REF_NAME} =~ [0-9]+.[0-9]+.x ]]; then
              EXAMPLES_BRANCH=${GITHUB_REF_NAME}
          elif [[ ${GITHUB_BASE_REF} =~ [0-9]+.[0-9]+.x ]]; then
              EXAMPLES_BRANCH=${GITHUB_BASE_REF}
          fi

          git clone --depth 1 --branch ${EXAMPLES_BRANCH} https://github.com/apache/camel-quarkus-examples.git \
            && cd camel-quarkus-examples \
            && echo "Current Examples commit:" $(git rev-parse HEAD) \
            && ./mvnw ${CQ_MAVEN_ARGS} ${BRANCH_OPTIONS} org.l2x6.cq:cq-maven-plugin:2.10.0:examples-set-platform -Dcq.camel-quarkus.version=${CQ_VERSION}

          # Filter examples for incremental builds
          MODULES_TO_BUILD="${EXAMPLE_MODULES//,/ }"

          if [ -n "${{ needs.initial-mvn-install.outputs.examples-filtered-matrix }}" ]; then
            echo "Incremental build detected - filtering examples based on affected extensions"

            # Get affected modules from filtered matrix
            AFFECTED_MODULES=$(echo '${{ needs.initial-mvn-install.outputs.examples-filtered-matrix }}' | jq -r '.modules[]')
            echo "Affected modules: $AFFECTED_MODULES"

            # Check if extensions-core/core is affected (build all examples if so)
            if echo "$AFFECTED_MODULES" | grep -q "^core$"; then
              echo "Core extension affected - building all examples"
            else
              # Filter examples based on dependency analysis
              echo "Affected modules for filtering: $AFFECTED_MODULES"
              FILTERED_MODULES=()

              for MODULE in ${EXAMPLE_MODULES//,/ }; do
                echo "Analyzing dependencies for example: $MODULE"
                cd ${MODULE}

                # Use quarkus:dependency-list which works per-module (root pom.xml is just an aggregator)
                echo "  Running: ../mvnw quarkus:dependency-list -DoutputFile=target/deps.txt -q"
                ../mvnw ${CQ_MAVEN_ARGS} quarkus:dependency-list -DoutputFile=target/deps.txt -q 2>&1 || true

                if [ -f target/deps.txt ]; then
                  echo "  ✓ deps.txt created ($(wc -l < target/deps.txt) lines)"
                  # Check if any affected extension appears in dependencies
                  SHOULD_BUILD=false
                  for AFFECTED in $AFFECTED_MODULES; do
                    # Convert module name to artifact ID (e.g., "box" -> "camel-quarkus-box")
                    ARTIFACT_ID="camel-quarkus-${AFFECTED}"
                    # Use word boundary matching to avoid partial matches (e.g., box matching dropbox)
                    if grep -qw "$ARTIFACT_ID" target/deps.txt; then
                      echo "  → Depends on affected extension: $ARTIFACT_ID"
                      SHOULD_BUILD=true
                      break
                    fi
                  done

                  if [ "$SHOULD_BUILD" == "true" ]; then
                    FILTERED_MODULES+=("$MODULE")
                  else
                    echo "  → No affected dependencies - skipping"
                  fi
                else
                  echo "  → Could not determine dependencies - including for safety"
                  FILTERED_MODULES+=("$MODULE")
                fi

                cd -
              done

              if [ ${#FILTERED_MODULES[@]} -eq 0 ]; then
                echo "No examples depend on affected extensions - skipping all"
                exit 0
              fi

              MODULES_TO_BUILD="${FILTERED_MODULES[@]}"
              echo "Filtered examples to build: $MODULES_TO_BUILD"
            fi
          fi

          BUILD_FAILURES=()

          for MODULE in $MODULES_TO_BUILD; do
            echo "Building example: $MODULE"
            cd ${MODULE}

            ../mvnw ${CQ_MAVEN_ARGS} clean verify \
              -Dformatter.skip -Dimpsort.skip \
              -Dquarkus.native.builder-image.pull=missing \
              -Pnative,docker,ci

            if [[ $? -ne 0 ]]; then
              BUILD_FAILURES[${#BUILD_FAILURES[@]}]=${MODULE}
            fi

            cd -
          done

          if [[ ${#BUILD_FAILURES[@]} -gt 0 ]]; then
            echo -e "\nBuild errors were encountered in the following projects:\n"
            for FAILURE in ${BUILD_FAILURES[@]}; do
                echo "* ${FAILURE}"
            done
            echo -e "\nCheck build logs for further information."
            exit 1
          fi
      - name: Report test failures
        uses: ./.github/actions/test-summary-report
        if: ${{ failure() }}
